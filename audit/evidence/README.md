# Evidence

Raw output backing findings in `../findings/`. One file per check: `{capability}-{date}.txt` (or `.png` for screenshots). Never edit after capture — if a check needs re-running, add a new dated file.
